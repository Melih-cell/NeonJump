{
  "mcpServers": {
    "mcp-unity": {
      "command": "uvx",
      "args": [
        "--from", "mcpforunityserver",
        "mcp-for-unity",
        "--transport", "stdio"
      ],
      "env": {
        "UNITY_MCP_HTTP_URL": "http://localhost:8080"
      }
    }
  }
}
